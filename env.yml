name: tsr
channels: [pytorch, nvidia, conda-forge]
dependencies:
  - python>=3.10
  - pytorch
  - torchvision
  - pytorch-cuda=12.1
  - pip
  - pip:
      - ultralytics==8.3.0
      - opencv-python
      - numpy
      - matplotlib
      - scikit-learn
      - pyyaml
      - tqdm
